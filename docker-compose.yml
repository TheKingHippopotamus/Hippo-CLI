version: '3.8'

services:
  hippocli:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hippocli
    volumes:
      - ./output:/app/output
      - ./config:/app/config
    environment:
      - PYTHONUNBUFFERED=1
    command: --help
    stdin_open: true
    tty: true

